# 《从「打字机」到「智能体」：拆开 AI 的装配线》生图提示词

本文共规划 **8 张图**：1 张全流程加工处理图 + 7 张环节图。正文已预留图片位置，文件名需与正文引用一致。

生成目标：使用 GPT Image 2 生成适合微信公众号科技科普文章的横版插画。除图 1 允许少量中文短标签外，其余图片优先生成**无文字版本**，关键中文标签后期叠加，避免乱码和变形文字。

---

## 全局统一风格

```text
为一篇中文微信公众号科技科普文章生成系列配图，主题是“从打字机到智能体：拆开 AI 的装配流程”。

统一视觉风格：现代扁平矢量 / 半扁平矢量插画，干净、专业、克制，有科技感但不冰冷。画面适合公众号正文阅读，不要做成海报广告，不要复杂 3D，不要霓虹赛博朋克，不要紫蓝渐变背景。

配色：背景使用暖白、浅灰白或极浅米色；主色使用暖橙、金橙、浅杏；辅色使用青蓝、蓝灰、深蓝灰；线条使用深蓝灰或深棕灰。整体色彩不要单一暖色，要有少量冷色用于技术层次和对比。

构图：横版 16:9，建议 1536x864 或 1792x1024。主体清晰，留白充足，适合后期叠加 1-3 个中文标签。不要生成大段文字，不要生成英文长句，不要出现乱码。除图 1 外，若需要文字，只能出现极少量短标签，但优先无文字。

角色与图标：AI 角色保持一致，可用简洁友好的小型机器人、发光方块大脑或抽象 AI 节点；工具、书本、数据库、箭头、齿轮、缓存、护栏等图标用统一线条风格。每张图都应服务概念解释，不做纯装饰。
```

---

## 图 1｜全流程加工处理图：从会吐字到能办事

**文件名**：`fig1-process-overview.png`

**正文位置**：文章标题下方，作为开篇总览图

**核心表达**：忽略技术细节，只保留每个环节的核心作用：生成文字 → 跑成服务 → 组织信息 → 请求执行 → 接入资料 → 循环推进 → 安全兜底。

```text
生成一张横版 16:9 的科技科普信息图，主题是“从会吐字到能办事：AI 智能体的全流程加工处理图”。

画面要像一张“加工处理流程图”，忽略实现细节，只保留每个环节的核心作用。左侧是一台简洁打字机或文本生成器，右侧是一个能连接工具和资料库的智能体系统。中间用一条清晰的横向流程连接 7 个环节，每个环节用一个大图标 + 一个短中文标签表达核心作用。

7 个短中文标签必须尽量清晰、简短，建议使用：
1. 生成文字
2. 跑成服务
3. 组织信息
4. 请求执行
5. 接入资料
6. 循环推进
7. 安全兜底

每个环节只保留一个核心视觉隐喻：
- 生成文字：打字机吐出小 token 方块
- 跑成服务：齿轮 / 服务器 / 加速箭头
- 组织信息：卡片汇入一个上下文窗口
- 请求执行：小纸条递给外部工具
- 接入资料：书本 / 数据库
- 循环推进：闭环箭头
- 安全兜底：护栏 / 人工确认 / 审计勾选

整张图要强调“从左到右逐步加工处理”的流程感，而不是复杂技术架构。每个节点之间用粗细统一的箭头连接，节点数量清晰，层级明确，读者一眼能看懂每一步解决什么问题。

允许少量中文短标签，但不要生成标题长句、解释句或段落文字。中文必须端正、清晰、无乱码；如果无法保证文字质量，请把标签区域留白，便于后期叠加。保持全局统一风格：暖白背景，暖橙为主，青蓝/蓝灰为辅，深蓝灰线条，扁平矢量，干净专业，留白充足。
```

---

## 图 2｜模型本体：只会一个 token 一个 token 往外吐

**文件名**：`fig2-typewriter-next-token.png`

**正文位置**：环节一

**核心表达**：模型像打字机，一次只生成一个 token；它不是查数据库，而是在续写。

```text
生成一张横版 16:9 的科技科普插画，主题是“模型像打字机：一次只吐出一个 token”。

画面主体是一台简洁、略带未来感的打字机或机械键盘装置，正在一格一格吐出小纸片 / 小方块。每个小方块代表一个 token，沿着纸带从左到右排列。打字机后方可以有一个柔和发光的抽象神经网络背景，表示大模型，但不要画成复杂电路。

画面需要表达两个概念：第一，它是逐步生成的，不是一整段答案瞬间出现；第二，它不是在数据库里查答案。可以在远处放一个模糊的“资料柜/数据库”图标，与打字机之间没有直接连接，以暗示“不是查库”。

风格保持统一：暖白背景，暖橙和浅杏为主，青蓝/蓝灰点缀，深蓝灰线条，扁平矢量，清晰留白。不要生成大段文字，不要生成乱码；如需标签，只预留上方或右侧空白。
```

---

## 图 3｜推理引擎：缓存、显存管理与并发服务

**文件名**：`fig3-inference-engine-cache.png`

**正文位置**：环节二

**核心表达**：推理引擎通过缓存和显存管理，让模型从单次生成变成高并发在线服务。

```text
生成一张横版 16:9 的机制示意插画，主题是“推理引擎让模型跑得快、能并发”。

画面中央是一台简洁的推理引擎机器，里面有齿轮和缓存块。左侧输入多条用户请求，右侧输出多条逐 token 生成的纸带。机器下方或内部有整齐排列的小内存块，表示 KV Cache 被分块管理；部分块被高亮，表现“缓存复用”和“显存管理”。

可以用一个小型仓库/货架类比显存管理：原本杂乱堆放的方块被整理成规则格子，多个请求可以共享空间。重点是让读者直观看到：不是模型突然更聪明，而是运行系统把计算和内存调度得更高效。

不要画成真实服务器机房，不要出现复杂公式。保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，干净专业。文字尽量不要生成，只预留标签位置。
```

---

## 图 4｜上下文编排：把任务、历史、规则和资料拼成临场剧本

**文件名**：`fig4-context-script.png`

**正文位置**：环节三

**核心表达**：模型每轮回答前，系统会把任务、历史、规则、工具说明和资料组织进上下文。

```text
生成一张横版 16:9 的科技科普插画，主题是“上下文编排：模型每轮看见什么”。

画面中央是一个 AI 节点或简洁机器人，面前有一份展开的“临场剧本”或工作台。四周有几类信息卡片被有序汇入剧本：用户任务、历史对话、系统规则、工具说明、检索资料。卡片经过一个漏斗或整理台，最后变成一份结构清晰的上下文剧本，递给 AI。

画面要表达“不是把所有东西乱塞进去，而是把下一步需要的信息组织好”。可以在一侧放少量灰色噪声卡片被挡在外面，表示过滤无关信息。不要画成复杂 prompt 文档，不要出现长文字。

保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，留白充足。不要生成大段文字；需要标签的区域留空，后期叠加中文。
```

---

## 图 5｜工具接口：模型点菜，外部代码下厨

**文件名**：`fig5-tool-call-menu.png`

**正文位置**：环节四

**核心表达**：模型不执行工具，只发出结构化请求；真正执行的是外部代码或服务端工具。

```text
生成一张横版 16:9 的机制示意插画，主题是“AI 调用工具像点菜单”。

画面用三步流程表达：左侧是 AI 角色坐在桌前，看着菜单并写下一张小纸条，象征“工具名 + 参数”的结构化请求；中间是纸条被送到后厨，后厨可以表现为代码窗口、齿轮机器或厨师，象征外部代码真正执行；右侧是结果被端回 AI 面前，变成一个结果卡片并回到上下文。

必须让读者一眼理解：AI 是点菜的顾客，不是下厨的厨师。AI 不要站在厨房里，不要拿锅铲。执行方要明显在 AI 之外。

保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，流程箭头清晰。不要生成大段文字；可预留“请求 / 执行 / 结果”三个短标签的位置，但文字后期叠加。
```

---

## 图 6｜知识与记忆：开卷资料库和可延续的笔记本

**文件名**：`fig6-rag-memory-library.png`

**正文位置**：环节五

**核心表达**：RAG 像开卷考试，记忆像笔记本；资料能查，状态能延续，但不保证绝对正确。

```text
生成一张横版 16:9 的科技科普插画，主题是“RAG 与记忆：一本参考书和一个笔记本”。

画面中央是 AI 角色或 AI 节点在完成任务。左侧是一座简洁资料库 / 书架，几页资料被检索出来流向 AI，象征 RAG 开卷查资料；右侧是一本笔记本或小型数据库，记录用户偏好、项目背景和长期状态，象征记忆。AI 面前有一份正在生成的回答。

画面需要保留边界感：可加入一个小小的“检查/问号”图标，表示资料可能检索错、需要核对。不要把 RAG 画成万能知识光环，也不要画成魔法书。

保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，干净专业。不要生成大段文字；中文标签后期叠加。
```

---

## 图 7｜闭环控制：观察、判断、行动、结果回流

**文件名**：`fig7-agent-loop.png`

**正文位置**：环节六

**核心表达**：智能体不是一次性回答，而是在观察、判断、行动、结果回流中持续推进任务。

```text
生成一张横版 16:9 的机制示意插画，主题是“智能体闭环：观察、判断、行动、结果回流”。

画面中央是一个简洁 AI 节点，外围是一圈清晰的循环箭头。循环中有四个节点：观察当前状态、判断下一步、调用工具行动、拿回结果；结果再回到观察。可以用放大镜、思考气泡、工具插头/扳手、结果卡片等图标表达，但不要复杂。

画面需要明确区分“一次性回答”和“持续推进”：可以在左下角用一个灰色的单向箭头小图标表示普通问答，在主画面用高亮闭环表示智能体循环。不要出现复杂流程图，不要有大段文字。

保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，留白充足。中文标签后期叠加，生图阶段尽量无文字。
```

---

## 图 8｜验收与护栏：权限边界、人工确认和审计记录

**文件名**：`fig8-guardrails-review.png`

**正文位置**：环节七

**核心表达**：智能体能运行不等于能托付，高风险动作需要边界、确认和审计。

```text
生成一张横版 16:9 的科技科普插画，主题是“智能体的验收与护栏”。

画面中有一个 AI 工作流正在接近高风险动作，例如发送邮件、付款、删除文件或发布代码。动作前方有清晰的护栏和检查点：人工确认按钮、权限边界、审计日志、沙箱区域。一个人类审核者坐在旁边查看结果，不要夸张，不要紧张恐怖风。

画面要表达：智能体能跑起来只是开始，真正可托付需要可控、可审计、有人兜底。可以用柔和的红橙色提醒风险，但整体仍保持专业、克制。

保持全局统一风格：暖白背景，暖橙主色，青蓝/蓝灰辅助，深蓝灰线条，扁平矢量，干净专业。不要生成大段文字；中文标签后期叠加。
```
